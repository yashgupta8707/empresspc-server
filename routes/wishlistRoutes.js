import express from 'express';
const router = express.Router();

// Wishlist routes placeholder
router.get('/', (req, res) => {
  res.json({ message: 'Wishlist routes not implemented yet' });
});

export default router;