import express from 'express';
const router = express.Router();

// Newsletter routes placeholder
router.get('/', (req, res) => {
  res.json({ message: 'Newsletter routes not implemented yet' });
});

export default router;